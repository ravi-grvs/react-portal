import { extendTheme } from '@chakra-ui/react'

const colors ={
    background: {
        ghostWhite: "#f5f5f7",
        lightGreen: '#d1ffbd'
      },
    palette:{

    }
}

const theme = extendTheme({ colors })

export default theme